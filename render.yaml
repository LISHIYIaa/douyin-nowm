services:
  - type: web
    name: douyin-nowm
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python douyin_nowm_web.py
    envVars:
      - key: PORT
        value: 10000
